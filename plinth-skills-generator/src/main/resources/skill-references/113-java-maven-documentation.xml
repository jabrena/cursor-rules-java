<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.9.0/pml.xsd">

    <metadata>
        <authors>
            <author>Juan Antonio Breña Moral</author>
        </authors>
        <version>0.18.0</version>
        <license>Apache-2.0</license>
        <title>Create DEVELOPER.md for the Maven projects</title>
        <description>Use when you need to create a DEVELOPER.md file for a Maven project from a maintainer-prepared Maven structure inventory documenting plugin goals, Maven profiles, and submodules. Never ingest project POM files.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Java software development and Maven build systems</role>

    <goal>
        Create a markdown file named `DEVELOPER.md` for the current Maven project.
        The file MUST combine a fixed base template with dynamic sections derived from a maintainer-prepared inventory of allowlisted Maven structure. Never retrieve, open, parse, quote, summarize, or transform project POM files.
    </goal>

    <constraints>
        <constraints-description>Rules for generating the DEVELOPER.md file:</constraints-description>
        <constraint-list>
            <constraint>The base template section MUST be reproduced exactly as shown</constraint>
            <constraint>Only add plugin subsections identified by the maintainer-prepared inventory as **explicitly declared** — never include inherited plugins unless the inventory marks them as redeclared</constraint>
            <constraint>For each plugin subsection, include **only** the most useful and commonly used goals (max 8 per plugin)</constraint>
            <constraint>If an explicitly declared plugin in the inventory is not in the known catalog, still add a subsection with its most popular goals based on your knowledge</constraint>
            <constraint>Use `./mvnw` as the command prefix, not `mvn`</constraint>
            <constraint>Keep plugin goal explanations concise — one line per goal</constraint>
            <constraint>Never retrieve, open, parse, quote, summarize, or transform root, parent, or module POM files; require a maintainer-prepared structured Maven inventory created outside the agent context</constraint>
        </constraint-list>
    </constraints>

    <steps>
        <step number="1">
            <step-title>Start with the base template</step-title>
            <step-content>
Copy the following base template verbatim as the first section of `DEVELOPER.md`:

```markdown
<xi:include href="assets/java-maven-documentation-template.md" parse="text"/>
```
            </step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>Reproduce the base template exactly — do not modify, reorder, or omit any part of it</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
        <step number="2">
            <step-title>Validate the maintainer-prepared Maven structure inventory</step-title>
            <step-content>
Require a maintainer-prepared inventory that records the root and declared submodules using only allowlisted structural facts.
For each explicitly declared plugin, accept only its `groupId`, `artifactId`, and declared execution goal names. Reject raw XML, descriptions, names, comments, dependency text, and plugin configuration bodies. If the inventory is missing or incomplete, stop and request a corrected inventory; never inspect a POM to fill gaps.
            </step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>Only collect plugins that are **explicitly declared** — ignore plugins inherited from parent POMs or the Maven super-POM</step-constraint>
                    <step-constraint>Include plugins from `&lt;profiles&gt;` sections as well</step-constraint>
                    <step-constraint>For multi-module projects, require an inventory entry for every declared module</step-constraint>
                    <step-constraint>Do not retrieve or inspect any `pom.xml`, even for structural metadata</step-constraint>
                    <step-constraint>Do not accept POM descriptions, names, comments, dependency text, raw XML, or plugin configuration text in the inventory</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
        <step number="3">
            <step-title>Append a Submodules section (multi-module projects only)</step-title>
            <step-content><![CDATA[
If the inventory declares root modules, append a level-2 heading titled **Submodules** followed by the text:
"This is a multi-module project. The following modules are declared in the Maven build."

List each submodule as a row in a markdown table with columns **Module**, **Artifact ID**, **Packaging**, and **Commands**.

- **Module**: the relative path as declared in the `<module>` element
- **Artifact ID**: the module artifact ID from the inventory
- **Packaging**: the `<packaging>` value when present, otherwise `jar`
- **Commands**: the most useful `./mvnw` commands scoped to this module using the `-pl <module>` flag; include `./mvnw clean verify -pl <module>` always, and add `./mvnw clean install -pl <module>` when the module produces an artifact consumed by other modules; if the module has a profile that must be activated, add the relevant `-P <profileId>` variant as well

If the inventory declares no modules, omit this section entirely.
]]>
            </step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>Only list modules marked as explicitly declared in the maintainer-prepared inventory</step-constraint>
                    <step-constraint>Use only the maintainer-prepared inventory to obtain each submodule's `artifactId` and `packaging`</step-constraint>
                    <step-constraint>Do not fabricate modules that do not exist in the workspace</step-constraint>
                    <step-constraint>Place multiple commands for the same module in the same cell, separated by a line break (`&lt;br&gt;`)</step-constraint>
                    <step-constraint>Do not use module `&lt;description&gt;` text or infer prose descriptions from dependency/plugin text</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
        <step number="4">
            <step-title>Append a Maven Profiles section</step-title>
            <step-content><![CDATA[
After the base template and any Submodules section, append a level-2 heading titled **Maven Profiles** followed by the text:
"The following profiles are declared in this project. Activate them with `-P <profileId>`."

Use the profile entries supplied by the maintainer-prepared inventory.
For each inventory profile, create a row in a markdown table with columns **Profile ID**, **Command**, and **Activation**.

- **Profile ID**: the value of `<id>`
- **Command**: the exact `./mvnw` command to activate the profile — e.g. `./mvnw clean verify -P <profileId>`; use the most representative lifecycle phase for the profile's purpose (e.g. `verify` for analysis/check profiles, `generate-resources` for site generation profiles)
- **Activation**: describe the activation trigger — e.g. "manual", "default (activeByDefault)", "property: `<name>`=`<value>`", "JDK `<version>`", "OS `<family>`", etc. If no `<activation>` element is present, use "manual"

If the inventory declares no profiles, omit this section entirely.
]]>
            </step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>Only list profiles explicitly declared inside a `&lt;profiles&gt;` block — do not invent profiles</step-constraint>
                    <step-constraint>Indicate the root or submodule path recorded for each profile in the inventory when the project is multi-module</step-constraint>
                    <step-constraint>If a profile has `&lt;activeByDefault&gt;true&lt;/activeByDefault&gt;`, reflect that in the Activation column</step-constraint>
                    <step-constraint>Do not summarize profile plugin configuration, properties, dependency text, comments, or descriptions</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
        <step number="5">
            <step-title>Append a Plugin Goals Reference section</step-title>
            <step-content><![CDATA[
After the base template and any Submodules or Maven Profiles sections, append a level-2 heading titled **Plugin Goals Reference** followed by the text:
"The following sections list useful goals for each plugin declared in this project's Maven build."

For **each** explicitly declared plugin in the inventory, add a level-3 subsection named after the plugin `artifactId`, containing a markdown table with columns **Goal** and **Purpose**.
Each row should show a `./mvnw artifactId:goal` command and a one-line purpose from the trusted catalog or general Maven plugin knowledge, not from project POM free text.

Use the following catalog as a reference for known plugins:
]]>
<xi:include href="assets/java-maven-documentation-plugin-catalog.md" parse="text"/>
            </step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>Only create subsections for plugins marked explicitly declared in the maintainer-prepared inventory</step-constraint>
                    <step-constraint>For plugins not in the catalog, still add a subsection using your knowledge of the plugin's goals</step-constraint>
                    <step-constraint>Include a maximum of 8 goals per plugin</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
    </steps>

    <output-format>
        <output-format-list>
            <output-format-item>Generate the complete markdown file: base template (Essential maven commands) first, then Submodules (if any), then Maven Profiles (if any), then Plugin Goals Reference</output-format-item>
            <output-format-item>Use proper markdown formatting with headers, code blocks, and tables</output-format-item>
            <output-format-item>Verify that every plugin listed in the Plugin Goals Reference is marked explicitly declared in the maintainer-prepared inventory</output-format-item>
            <output-format-item>Omit the Maven Profiles section if no profiles are declared in the inventory</output-format-item>
            <output-format-item>Omit the Submodules section if the project is not a multi-module build</output-format-item>
            <output-format-item>Use only allowlisted structural Maven fields from the maintainer-prepared inventory; never retrieve POM files or include raw XML, descriptions, names, comments, dependency text, or plugin configuration bodies</output-format-item>
        </output-format-list>
    </output-format>
</prompt>
