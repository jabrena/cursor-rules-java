<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.9.0/pml.xsd">

    <metadata>
        <authors>
            <author>Juan Antonio Breña Moral</author>
        </authors>
        <version>0.19.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>ISO/IEC 25010:2023 Quality Model Guidance for Java Enterprise Engineering</title>
        <description>Use as Java-focused ISO/IEC 25010:2023 engineering examples covering all nine product-quality characteristics for structured, repeatable Java Enterprise quality-attribute review.</description>
    </metadata>

    <role>You are a senior Java enterprise architect and quality reviewer who translates ISO/IEC 25010:2023 product-quality characteristics into concrete Java engineering examples and reviewable evidence patterns</role>

    <goal>
        Apply these ISO/IEC 25010:2023-aware examples after the chapters-summary reference has been read and the target Java enterprise system's review scope is understood.

        These examples are not certification advice, compliance advice, conformity advice, or audit conclusions. They show engineering patterns that help Java teams create reviewable evidence for architecture, product, security, platform, operations, and accountable business owners.

        Use this examples reference together with `references/814-regulations-iso-25010-chapters-summary.md`.
    </goal>

    <constraints>
        <constraints-description>
            Translate ISO/IEC 25010:2023 quality characteristics into engineering controls and evidence for Java enterprise systems without replacing qualified architecture, product, security, or business-owner review, and without producing certification, compliance, conformity, or audit conclusions.
        </constraints-description>
        <constraint-list>
            <constraint>**NOT CERTIFICATION, COMPLIANCE, OR CONFORMITY ADVICE**: Treat the examples as engineering control patterns and escalation aids, not certification readiness, compliance approval, audit findings, or final conformity decisions</constraint>
            <constraint>**EVIDENCE FIRST**: Prefer reviewable evidence over claims that a quality characteristic is satisfied</constraint>
            <constraint>**NINE-CHARACTERISTIC COVERAGE**: Match examples to all nine characteristics — Functional Suitability, Performance Efficiency, Compatibility, Interaction Capability, Reliability, Security, Maintainability, Flexibility, and Safety</constraint>
            <constraint>**OWNER HANDOFFS**: Include architecture, product, security, platform, operations, and accountable business owners in control records when a finding requires a decision beyond engineering review</constraint>
            <constraint>**RELEASE READINESS**: Do not mark a Java enterprise system quality-reviewed when scope, evidence, or per-characteristic findings are undocumented, untested, or unresolved</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="functional-suitability-traceability">
            <example-header>
                <example-title>Trace acceptance criteria to implementation and tests</example-title>
                <example-subtitle>Functional Suitability: completeness, correctness, appropriateness</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern to prove that acceptance criteria map to concrete controller/service methods and automated tests, and that domain-model edge cases are covered.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@Test
void rejectsOrderWhenInventoryInsufficient() {
    Order order = OrderFixtures.orderFor(Sku.of("SKU-42"), 5);
    inventory.setAvailable(Sku.of("SKU-42"), 2);

    OrderResult result = orderService.place(order);

    assertThat(result.status()).isEqualTo(OrderStatus.REJECTED_INSUFFICIENT_INVENTORY);
    assertThat(result.reasonCode()).isEqualTo("INVENTORY_INSUFFICIENT");
}

@Test
void acceptsOrderAtExactAvailableQuantityBoundary() {
    Order order = OrderFixtures.orderFor(Sku.of("SKU-42"), 2);
    inventory.setAvailable(Sku.of("SKU-42"), 2);

    OrderResult result = orderService.place(order);

    assertThat(result.status()).isEqualTo(OrderStatus.ACCEPTED);
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@Test
void orderWorks() {
    OrderResult result = orderService.place(OrderFixtures.anyOrder());
    assertThat(result).isNotNull();
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="performance-efficiency-capacity-evidence">
            <example-header>
                <example-title>Detect N+1 queries and document capacity limits</example-title>
                <example-subtitle>Performance Efficiency: time behaviour, resource utilization, capacity</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing data access and load-test evidence. Fetch joins or explicit batch loading avoid N+1 query patterns, and capacity limits are documented rather than assumed.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@Query("""
    SELECT o FROM Order o
    JOIN FETCH o.lineItems li
    JOIN FETCH li.product
    WHERE o.customerId = :customerId
    """)
List<Order> findOrdersWithLineItemsByCustomer(@Param("customerId") Long customerId);

// capacity evidence recorded alongside the load test suite
// docs/performance/order-service-load-test-2026-08-01.md:
//   p95 latency: 180ms at 250 rps sustained for 30 minutes
//   documented capacity limit: 300 rps before connection-pool saturation]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[List<Order> orders = orderRepository.findByCustomerId(customerId);
for (Order order : orders) {
    order.getLineItems().forEach(li -> li.getProduct().getName()); // lazy-loads per item
}
// no load test evidence, no documented capacity limit]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="compatibility-api-versioning">
            <example-header>
                <example-title>Version and deprecate API contracts safely</example-title>
                <example-subtitle>Compatibility: co-existence, interoperability</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing REST or message contracts that must co-exist with other deployed versions and interoperate with existing consumers.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@RestController
@RequestMapping("/api/v2/orders")
class OrderControllerV2 {

    @GetMapping("/{orderId}")
    OrderResponseV2 getOrder(@PathVariable String orderId) {
        return orderQueryService.findV2(orderId);
    }
}

// v1 remains available and documented as deprecated with a sunset date
@RestController
@RequestMapping("/api/v1/orders")
@Deprecated(since = "2026-06-01", forRemoval = true)
class OrderControllerV1 {
    // Sunset: 2027-01-01. Migration guide: docs/api/order-v1-to-v2-migration.md
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@RestController
@RequestMapping("/api/orders")
class OrderController {
    // breaking field rename shipped directly into the only version,
    // no deprecation window, no migration guide
    @GetMapping("/{orderId}")
    OrderResponse getOrder(@PathVariable String orderId) { ... }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="interaction-capability-self-descriptive-errors">
            <example-header>
                <example-title>Return self-descriptive, learnable API errors</example-title>
                <example-subtitle>Interaction Capability: recognizability, learnability, operability, user error protection, self-descriptiveness</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing input validation and error handling. Clients and operators should receive clear, actionable 4xx responses instead of stack traces or ambiguous 500s.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@ExceptionHandler(ConstraintViolationException.class)
ResponseEntity<ApiError> handleValidation(ConstraintViolationException ex) {
    ApiError error = ApiError.builder()
            .code("VALIDATION_FAILED")
            .message("One or more fields are invalid")
            .fieldErrors(ex.getConstraintViolations().stream()
                    .map(v -> new FieldError(v.getPropertyPath().toString(), v.getMessage()))
                    .toList())
            .documentationLink("https://docs.example.com/errors/VALIDATION_FAILED")
            .build();
    return ResponseEntity.badRequest().body(error);
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@ExceptionHandler(Exception.class)
ResponseEntity<String> handleAny(Exception ex) {
    return ResponseEntity.status(500).body(ex.toString()); // leaks stack trace, no guidance
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="reliability-resilience-patterns">
            <example-header>
                <example-title>Apply circuit breakers, retries, and timeouts on outbound calls</example-title>
                <example-subtitle>Reliability: faultlessness, availability, fault tolerance, recoverability</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing outbound HTTP or messaging calls, health/readiness probes, and recovery procedures.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@CircuitBreaker(name = "paymentGateway", fallbackMethod = "fallbackAuthorize")
@Retry(name = "paymentGateway")
@TimeLimiter(name = "paymentGateway")
CompletableFuture<AuthorizationResult> authorize(PaymentRequest request) {
    return CompletableFuture.supplyAsync(() -> paymentClient.authorize(request));
}

AuthorizationResult fallbackAuthorize(PaymentRequest request, Throwable t) {
    log.warn("Payment gateway unavailable, queuing for idempotent retry: {}", request.idempotencyKey());
    return AuthorizationResult.queuedForRetry(request.idempotencyKey());
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[AuthorizationResult authorize(PaymentRequest request) {
    return paymentClient.authorize(request); // no timeout, no retry, no circuit breaker
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="security-authz-and-secrets">
            <example-header>
                <example-title>Enforce authorization and keep secrets out of source and logs</example-title>
                <example-subtitle>Security: confidentiality, integrity, non-repudiation, accountability, authenticity, resistance</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing endpoint authorization, secrets handling, and audit logging for accountability.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[@PreAuthorize("hasRole('ORDER_ADMIN') and #orderId == authentication.details.orderId")
@PostMapping("/{orderId}/cancel")
ResponseEntity<Void> cancelOrder(@PathVariable String orderId, Authentication authentication) {
    auditLog.record(AuditEvent.of("ORDER_CANCEL", authentication.getName(), orderId));
    orderService.cancel(orderId);
    return ResponseEntity.noContent().build();
}

// secrets loaded from an approved secret store, never hardcoded
@Value("${payment.gateway.api-key}")
private String apiKey; // sourced from Vault/Secrets Manager, never committed]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@PostMapping("/{orderId}/cancel")
ResponseEntity<Void> cancelOrder(@PathVariable String orderId) {
    // no authorization check
    orderService.cancel(orderId);
    return ResponseEntity.noContent().build();
}

private static final String API_KEY = "sk_live_51H8x..."; // hardcoded secret]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="maintainability-module-boundaries">
            <example-header>
                <example-title>Keep module boundaries and test pyramid healthy</example-title>
                <example-subtitle>Maintainability: modularity, reusability, analysability, modifiability, testability</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing package structure, coupling, and the shape of the automated test suite.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="markdown"><![CDATA[Maintainability review notes

- Module boundaries: `order-domain` has no dependency on `order-web`; `order-web` depends on `order-domain` only through a port interface.
- Coupling: static-analysis afferent/efferent coupling report attached; no cyclic package dependencies detected.
- Test pyramid: 420 unit tests, 65 integration tests, 12 acceptance tests; ratio consistent with the project's testing strategy.
- Testability: domain services take collaborators through constructor injection; no static singletons in business logic.
- Complexity: cyclomatic complexity report shows no method above the agreed threshold of 10.]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="markdown"><![CDATA[Maintainability review notes

- Everything lives in one `util` package.
- OrderService reaches directly into the web layer's static helper classes.
- No test-pyramid or complexity evidence attached.]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="8" id="flexibility-configuration-externalization">
            <example-header>
                <example-title>Externalize configuration and keep services stateless for horizontal scaling</example-title>
                <example-subtitle>Flexibility: adaptability, scalability, installability, replaceability</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern when reviewing horizontal-scaling readiness, infrastructure-as-code, and the replaceability of third-party integrations.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="yaml"><![CDATA[# application.yml — externalized per environment, no in-code environment branching
payment:
  gateway:
    base-url: ${PAYMENT_GATEWAY_URL}
    provider: ${PAYMENT_GATEWAY_PROVIDER:stripe} # swappable via config, not code change

server:
  session:
    store: none # stateless; session state kept in shared cache, safe to scale horizontally]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[String gatewayUrl = env.equals("prod")
        ? "https://payments.prod.internal"
        : "https://payments.staging.internal"; // hardcoded per-environment branching in code

// in-memory session map on the instance — cannot scale horizontally without sticky sessions
private static final Map<String, Session> SESSIONS = new ConcurrentHashMap<>();]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="9" id="safety-fail-safe-hazard-warning">
            <example-header>
                <example-title>Default to fail-safe and surface hazard warnings before irreversible actions</example-title>
                <example-subtitle>Safety: operational constraint, risk identification, fail safe, hazard warning, safe integration</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use this pattern for Java systems with real-world operational effects, where an action is irreversible or high-impact — for example bulk data deletion, financial settlement, or automated infrastructure changes.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[RefundResult processRefund(RefundRequest request) {
    if (request.amount().compareTo(request.originalCharge().amount()) > 0) {
        throw new HazardousOperationException(
                "Refund amount exceeds original charge; blocked pending manual review",
                "REFUND_EXCEEDS_ORIGINAL_CHARGE");
    }
    if (!request.hasApproval() && request.amount().isGreaterThan(Money.of(1000, "USD"))) {
        return RefundResult.pendingApproval(request.id(), "High-value refund requires approver sign-off");
    }
    return refundGateway.execute(request); // proceeds only once guardrails pass
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[RefundResult processRefund(RefundRequest request) {
    return refundGateway.execute(request); // no bounds check, no approval gate, no hazard warning
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**MATCH** the relevant example patterns for each of the nine ISO/IEC 25010:2023 characteristics under review: functional-suitability traceability, performance-efficiency capacity evidence, compatibility API versioning, interaction-capability self-descriptive errors, reliability resilience patterns, security authorization and secrets, maintainability module boundaries, flexibility configuration externalization, or safety fail-safe and hazard-warning controls</output-format-item>
            <output-format-item>**RECOMMEND** engineering controls: acceptance-criteria traceability, load/capacity test evidence, API versioning and deprecation records, self-descriptive error handling, resilience patterns, authorization and secrets management, module-boundary and test-pyramid review, configuration externalization, and fail-safe/hazard-warning guardrails</output-format-item>
            <output-format-item>**REPORT** conclusions as engineering evidence and action items, never as certification advice, compliance advice, conformity advice, or audit conclusions</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**OWNER ESCALATION**: Treat ISO/IEC 25010:2023 certification, compliance, and conformity determinations as qualified owner decisions, not engineering conclusions</safeguards-item>
            <safeguards-item>**EVIDENCE OVER CLAIMS**: Never mark a quality characteristic satisfied without reviewable evidence; record it as an evidence gap instead</safeguards-item>
            <safeguards-item>**SCOPE DISCIPLINE**: Base findings only on the Java system, module, or delivery pipeline actually under review; do not generalize findings across unrelated systems</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
